
  // Single place this tool points at the site it edits. Change this (and
  // the matching ALLOWED_PREVIEW_ORIGINS entry in website/assets/js/theme-apply.js)
  // if you run the website server on a different port.
  window.WEBSITE_ORIGIN = 'http://localhost:4173';
